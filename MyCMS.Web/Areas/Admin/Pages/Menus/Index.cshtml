@page
@model MyCMS.Web.Areas.Admin.Pages.Menus.IndexModel
@{
    ViewData["Title"] = "Menus";
    Layout = "_AdminLayout";
}

<h1>Menus</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New Menu</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Menu Name</th>
            <th>Position</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Menus != null)
        {
            @foreach (var menu in Model.Menus)
            {
                <tr>
                    <td>@menu.MenuName</td>
                    <td>@menu.Position</td>
                    <td>@menu.MenuDesc</td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@menu.Id" class="btn btn-sm btn-primary">Edit</a>
                        <a asp-page="./Delete" asp-route-id="@menu.Id" class="btn btn-sm btn-danger">Delete</a>
                        <a asp-page="./Items" asp-route-menuId="@menu.Id" class="btn btn-sm btn-info">Manage Items</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
